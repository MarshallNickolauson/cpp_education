#include <iostream>

int main() {

    // Order of operations
    // 1. Parenthesis
    // 2. * % /
    // 3. + & -

    int students = 6 - (5 + 4) * 3 / 2;

    std::cout << students;

    // int students = 20;
    // students = students + 1;
    // students += 1;
    // students++;

    // students--;

    // students = students * 2;
    // students *= 2;

    // students = students / 2;
    // students /= 2;
    
    // int remainder = students % 2;

    // std::cout << remainder;

    return 0;
}